---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes

- name: Restart Jenkins
  ansible.builtin.systemd:
    name: jenkins
    enabled: yes
    state: restarted

- name: Reload Nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
